<!DOCTYPE html>
<html lang="zh-cn">
	<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
	<title><%= @page_title %></title>
	<link rel="stylesheet" href="<%= $SITE_URL %>/css/user.css">
	<script type="text/javascript" src="http://tajs.qq.com/stats?sId=25502196" charset="UTF-8"></script>
	<%= "<meta name=\"description\" content=\"#{@meta_description}\" />" unless @meta_description.nil? %>
	<%= "<meta name=\"keywords\" content=\"#{@meta_keywords}\" />" unless @meta_keywords.nil? %>
	</head>

	<body>
	<div id="page">
	
		<header>
		<hgroup>
			<h1 id="site-title"><a href="<%= $SITE_URL %>" title="<%= @site_title %>"><%= @site_title %></a></h1>
			<p id="site-description"><%= @site_description %></p>
		</hgroup>

			<nav>	
				<%= @nav %>
			</nav>
		</header>

		<div id="page-content">
			<%= @content %>
			<%= @page_bar unless @page_bar.nil? %>
			<%= @comment unless @comment.nil? %>
			<%= @comment_page_bar unless @comment_page_bar.nil? %>
			<%= @comment_form unless @comment_form.nil? %>
		</div>

		<aside>
			<ul><p class="menu-title">分类目录</p>
				<%= @category_list %>
			</ul>
		</aside>

		<footer>
			<p><%= @site_footer %></p>
		</footer>
	</div>
	</body>
</html>